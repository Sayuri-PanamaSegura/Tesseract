---
import Layout from "../layouts/Layout.astro";
---

<Layout ocultarSuscripcion={true}>
  <section id="contacto"  class="py-24 px-6 pt-32">
  <div class="max-w-6xl mx-auto mb-6">
    <a href="/" class="flex items-center text-sm hover:underline transition" style="black">
      <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7" />
      </svg>
      
    </a>
  </div>

    <div class="max-w-3xl mx-auto text-center mb-12">
      <h1 class="text-3xl md:text-4xl font-bold mb-4 text-white">Contacta con nosotros</h1>
      <p class="text-white text-base md:text-lg">
        ¿Buscas proteger tus datos, accesos o infraestructura crítica? Déjanos un mensaje y te ayudamos a encontrar la solución ideal.
      </p>
    </div>

    <form 
     action="https://formsubmit.co/marketing@conecta.mx"
      method="POST"
      class="max-w-2xl mx-auto  backdrop-blur-lg p-10 rounded-3xl border-2 border-[#CEFF1C] shadow-xl grid gap-6"
  >
      <input type="hidden" name="_captcha" value="false">
      <input type="hidden" name="_next" value="/gracias">

      <!-- Nombre y Apellido -->
      <div class="grid md:grid-cols-2 gap-4">
        <div>
          <label class="flex items-center gap-2 text-sm font-medium  text-white mb-1">
             Nombre
          </label>
          <input type="text" name="nombre" required placeholder="Ej. María" class="w-full px-4 py-3  border-2 border-[#CEFF1C] rounded-xl focus:outline-none focus:ring-2 focus:black transition" />
        </div>
        <div>
          <label class="flex items-center gap-2 text-sm font-medium text-white mb-1">
            Apellido
          </label>
          <input type="text" name="apellido" required placeholder="Ej. López" class="w-full px-4 py-3  border-2 border-[#CEFF1C] rounded-xl focus:outline-none focus:ring-2 focus:black transition" />
        </div>
      </div>

      <!-- Correo -->
      <div>
        <label class="flex items-center gap-2 text-sm font-medium text-white mb-1">
           Correo electrónico
        </label>
        <input type="email" name="email" required placeholder="tucorreo@email.com" class="w-full px-4 py-3  border-2 border-[#CEFF1C] rounded-xl focus:outline-none focus:ring-2 focus:black transition" />
      </div>

      <!-- Teléfono o WhatsApp -->
      <div>
        <label class="flex items-center gap-2 text-sm font-medium text-white mb-1">
           Teléfono / WhatsApp
        </label>
        <input type="tel" name="telefono" placeholder="Ej. +52 55 1234 5678" class="w-full px-4 py-3  border-2 border-[#CEFF1C] rounded-xl focus:outline-none focus:ring-2 focus:black transition" />
      </div>

      <!-- Empresa -->
      <div>
        <label class="flex items-center gap-2 text-sm font-medium text-white mb-1">
         Empresa
        </label>
        <input type="text" name="empresa" placeholder="Nombre de la empresa" class="w-full px-4 py-3 border-2  border-[#CEFF1C] rounded-xl focus:outline-none focus:ring-2 focus:black transition" />
      </div>

      <!-- Mensaje -->
      <div>
        <label class="flex items-center gap-2 text-sm font-medium text-white mb-1">
           ¿Cómo podemos ayudarte?
        </label>
        <textarea 
          name="mensaje" 
          rows="4" 
          placeholder="Describe brevemente tu necesidad o proyecto..." 
          class="w-full px-4 py-3  border-2 border-[#CEFF1C] rounded-xl focus:outline-none focus:ring-2 focus:black transition resize-none"
        ></textarea>
      </div>

      <!-- Botón negro animado -->
         <button
        type="submit"
        class="btn-neon px-8 py-3  font-bold rounded-xl "
      
        >ENVIAR MENSAJE
    </form>
  </section>
</Layout>
